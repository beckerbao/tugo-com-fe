# Bật chế độ Rewrite
RewriteEngine On

# Ngăn chặn liệt kê thư mục
Options -Indexes

# Redirect truy cập gốc tới pages/home.php
RewriteRule ^$ pages/home.php [L]

# Chặn truy cập trực tiếp vào file .htaccess
<Files .htaccess>
    Order Allow,Deny
    Deny from all
</Files>
